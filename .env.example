# Anthropic API key — required for the Baby Name Chooser (2MOD.5).
# Get yours at: https://console.anthropic.com/settings/keys
# This is a server-side secret; it is read via $env/static/private in SvelteKit
# and never sent to the browser. Never commit the real key.
ANTHROPIC_API_KEY=
